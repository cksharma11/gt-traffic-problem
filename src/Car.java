import java.util.List;

public class Car extends Vehicle {
    public Car(double speed, double cratersCrossCount, List<WeatherType> supportedWeatherTypes) {
        super(speed, cratersCrossCount, supportedWeatherTypes);
    }
}
