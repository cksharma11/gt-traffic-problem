import java.util.List;

public class Car extends Vehicle {
    public Car(double speed, double creatorCrossTime, List<Weather> supportedWeathers) {
        super(speed, creatorCrossTime, supportedWeathers);
    }
}
